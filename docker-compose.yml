version: '3.8'

services:
  wedding-website:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./uploads:/app/public/uploads
      - ./data:/app/data  # Add this line!
    restart: unless-stopped