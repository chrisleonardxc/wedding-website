# Copy this file to .env (for docker-compose) or .env.local (for `npm run dev`)
# and fill in real values. Never commit the filled-in file.

# Password guests use to access the site
SITE_PASSWORD=

# Password required to delete photos / view prediction results
ADMIN_PASSWORD=

# Optional: custom path to the SQLite database
# DB_PATH=./data/wedding.db
